# BO method module: standard Gaussian-process BO.
#
# Best for lower-dimensional parameter spaces. For 41D BTAH, turbo.yaml is
# usually safer; use GP after fixing/tying parameters to reduce dimension.

include:
  - turbo.yaml

optimization:
  method: "gp"
  n_initial: 32
  n_bo_iterations: 200

