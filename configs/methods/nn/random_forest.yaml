# Surrogate module: random forest.
#
# Useful as a robust non-neural baseline when the dataset is small or noisy.

nn:
  enabled: true
  model: "random_forest"
  physics_features: true
  train_top_fraction: 0.10
  prefer_stable_results: true

