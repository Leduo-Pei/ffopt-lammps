# Surrogate module: Gaussian-process model.
#
# Useful for smaller BO datasets and lower-dimensional subspaces.

nn:
  enabled: true
  model: "gp"
  physics_features: true
  train_top_fraction: 0.10
  prefer_stable_results: true

